# Only needed when you put the dashboard online. Locally, leave it unset: dev is open.
DASHBOARD_PASSWORD=
# Where the SQLite file lives. Default is data/dealer.db inside the project.
DATABASE_PATH=data/dealer.db
